/*The following statement should determine if count is outside the range of 0 through 100. What is wrong with it?

if (count<0 && count >100)
    */
count = 0;
if (count < 0 || count > 100)
{
    console.log ("CHeck");
}